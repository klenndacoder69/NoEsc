# NoEsc SUID/SGID Whitelist Configuration
# 
# The daemon automatically trusts core system paths:
# /bin/, /usr/bin/, /sbin/, /usr/sbin/, /usr/lib/, /usr/libexec/
#
# Add any CUSTOM directories below (one per line). 
# Binaries executing from these directories will NOT trigger an SUID abuse alert.

/opt/student_sandbox/
